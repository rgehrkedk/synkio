{
  "name": "synkio-demo-app",
  "version": "1.0.0",
  "private": true,
  "description": "Demo app showcasing Synkio design tokens integration",
  "scripts": {
    "dev": "npx serve src",
    "pull": "npx synkio pull",
    "build:tokens": "npx synkio build",
    "sync": "npx synkio pull && npx synkio build",
    "docs": "npx synkio docs --open",
    "validate": "npx synkio validate"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
