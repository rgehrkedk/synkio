{
  "name": "synkio-sync-plugin",
  "version": "1.0.0",
  "description": "Simple Figma plugin to sync variables for Synkio CLI",
  "scripts": {
    "build": "esbuild code.ts --bundle --outfile=code.js --target=es2020",
    "watch": "esbuild code.ts --bundle --outfile=code.js --target=es2020 --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
